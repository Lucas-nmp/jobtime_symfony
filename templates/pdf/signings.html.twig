<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Fichajes PDF</title>
</head>
<body>
    <h1>Reporte de Fichajes</h1>
    <p>Usuario: {{ userName }}</p>

    <ul id="signingsList">
        {% for signing in signings %}
            <li>
                Fecha: {{ signing.datetime|date("d-m-Y") }} - Hora: {{ signing.datetime|date("H:i:s") }} - 
                {% if signing.entry == 1 %}
                    Entrada
                {% elseif signing.entry == 0 %}
                    Salida
                {% endif %}
                - {{ signing.type }}
            </li>
        {% endfor %}
    </ul>

    <div class="mt-3">
        <h5>Total horas trabajadas el {{ formattedDate }}: {{ totalHours }}</h5>
    </div>
</body>
</html>

